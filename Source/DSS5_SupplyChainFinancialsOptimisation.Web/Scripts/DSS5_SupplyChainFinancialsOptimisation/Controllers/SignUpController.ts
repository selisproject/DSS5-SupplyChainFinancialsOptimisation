// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
$(document).keyup((e) => {
    if (e.which === 13 && Joove.Common.eventPreventsDefaultFormAction(e as JQueryEventObject) === false && !$(e.target).parent().hasClass("search-element")) {
        window._popUpManager.previouslyFocusedElement = document.activeElement;
        $(":focus").blur();
        Joove.Core.getScope().eventCallbacks.Button1Clicked(e);
    }
});
window.onbeforeunload = (e) => {
    if (window._context.isDirty) {
        var msg = window._resourcesManager.getGlobalResource("RES_WEBFORM_DirtyMessage");
        e.returnValue = msg;     // Gecko, Trident, Chrome 34+
        return msg;
    }
};
interface Window { scope_SignUp: DSS5_SupplyChainFinancialsOptimisation.Controllers.ISignUpScope; }
namespace DSS5_SupplyChainFinancialsOptimisation.Controllers {
const SignUpConditionalFormattings = [
                                     ];
const SignUpDataValidations = [
                              ];
const SignUpCalculatedExpressions = [
                                    ];
// Manual Data for txtChoosenRole
window["dataFortxtChoosenRole"] = [ {
_key: "Supplier",
_text: window._resourcesManager.getComboBoxManualValueDisplay('BussinessTypes', '0', false , null),
_default: false
}, {
_key: "Investor",
_text: window._resourcesManager.getComboBoxManualValueDisplay('BussinessTypes', '1', false , null),
_default: false
}, {
_key: "Buyer",
_text: window._resourcesManager.getComboBoxManualValueDisplay('BussinessTypes', '2', false , null),
_default: false
} ]

// Datasets Column Info
export var BussinessTypes_ColumnInfo = [new Joove.ColumnInfo("Value", "string", null,  true)];
window["BussinessTypes_ColumnInfo"] = BussinessTypes_ColumnInfo;

// Datasets Projection Shemas



export interface ISignUpScope extends Joove.IWebPageScope {
model:
    DSS5_SupplyChainFinancialsOptimisation.ViewModels.SignUp.SignUpViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,

    },
    _masterValidations?: any;
}

class SignUpController {
private timeoutDelayLockButton1Clicked:
    any;


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_SignUp.model = window.scope_SignUp.dehydrate();
        //window.scope_MasterPage.model = window.scope_SignUp.model;
        DSS5_SupplyChainFinancialsOptimisation.ViewModels.SignUp.SignUpViewModel._lightCast(window.scope_SignUp.model);
        // Joove.Common.applyScope(window.scope_SignUp);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: ISignUpScope, $timeout: ng.ITimeoutService) {
        window.scope_SignUp = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DSS5_SupplyChainFinancialsOptimisation.ViewModels.SignUp.SignUpViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_MasterPage._validations;
        $scope._validations = { summary:
                                [],
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: ISignUpScope, $timeout: ng.ITimeoutService) {


        $scope.model = DSS5_SupplyChainFinancialsOptimisation.ViewModels.SignUp.SignUpViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_MasterPage.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);


        //}

        $.connection['eventsHub'].on('__connectedEvent');

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {
        };
// Controller actions
        $scope.actions = {
Render:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("SignUp", "Render", "GET", [], {}, _modalOptions);
            },
SignUp:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();
                await window._ruleEngine.update(Joove.EvaluationTimes.OnSubmit);

                window._context.isDirty = false;
                Joove.Core.executeControllerActionNew({
verb: 'POST', controller: 'SignUp', action: 'SignUp',
queryData: [], postData: {'model': $scope.model},
cb: _cb, modalOptions: _modalOptions, event: _e
                });
            },
IntermediaryRender:
            async (id, _cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("SignUp", "IntermediaryRender", "GET", [id], {}, _modalOptions);
            },
        };
// Events
        $scope.eventCallbacks = {
Button1Clicked:
            (e, DataItem, _parents) => {
                window._recorder.addState(e);

                if (e != null) {
                    e.stopPropagation();
                    e.preventDefault();
                }

                if (this.timeoutDelayLockButton1Clicked != null) {
                    $timeout.cancel(this.timeoutDelayLockButton1Clicked);
                }

                this.timeoutDelayLockButton1Clicked = $timeout(() => {
                    Joove.Common.setControlKeyPressed(e, 0); Joove.Common.setLastClickedElement(e);

                    Joove.Validation.Manager.validateFormAndExecute(() => {
                        $scope.actions.SignUp(null, null, e);
}, { groups: [Joove.Validation.Constants.ALL_GROUPS], withDataValidationsCheck: true, withRequiredFieldsCheck: true });
                }, 0);
            },

        };

// Rules
        window._ruleEngine.addDataValidations(SignUpDataValidations);
        window._ruleEngine.addConditionalFormattings(SignUpConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(SignUpCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_MasterPage as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("SignUpController", ["$scope", "$timeout", SignUpController] as Array<string>);
}
