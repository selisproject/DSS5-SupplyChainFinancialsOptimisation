// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
window.onbeforeunload = (e) => {
    if (window._context.isDirty) {
        var msg = window._resourcesManager.getGlobalResource("RES_WEBFORM_DirtyMessage");
        e.returnValue = msg;     // Gecko, Trident, Chrome 34+
        return msg;
    }
};
interface Window { scope_TrackingMapForm: DSS5_SupplyChainFinancialsOptimisation.Controllers.ITrackingMapFormScope; }
namespace DSS5_SupplyChainFinancialsOptimisation.Controllers {
const TrackingMapFormConditionalFormattings = [
        ];
const TrackingMapFormDataValidations = [
                                       ];
const TrackingMapFormCalculatedExpressions = [
        ];

// Datasets Column Info
export var MapPointDataset_ColumnInfo = [new Joove.ColumnInfo("Latitude", "double", null, false),
           new Joove.ColumnInfo("Longitude", "double", null, false),
           new Joove.ColumnInfo("Title", "string", null, false),
           new Joove.ColumnInfo("Icon", "string", null, false)];
window["MapPointDataset_ColumnInfo"] = MapPointDataset_ColumnInfo;

export class TrackingMapFormMapPointDatasetViewModelDataset extends Joove.IViewModelDataset<any> {


    constructor(model, partialViewControlName: string = null) {
        super(model, partialViewControlName);
        this.limit = 0;
    }

    Filter(inputs: any): (DataItem: any, index: number, items: Array<any>) => boolean {
        var $scope = { model: this.model };

        return (DataItem, index, items) => {
            return true;


        };
    }

    PackInputs($control: JQuery): any {
        return Joove.DatasourceManager.getDatasetControlInputs($control);
    }

    Sort(items: Joove.ViewModelCollection): Joove.ViewModelCollection {
        return items;
    }

    GetSource(parents: number[]): Joove.ViewModelCollection {
        return this.model.MapPoints;
    }


}



// Datasets Projection Shemas



export interface ITrackingMapFormScope extends Joove.IWebPageScope {
model:
    DSS5_SupplyChainFinancialsOptimisation.ViewModels.TrackingMapForm.TrackingMapFormViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,

    },
    _masterValidations?: any;
}

class TrackingMapFormController {
private timeoutDelayLockButtonClicked:
    any;


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_TrackingMapForm.model = window.scope_TrackingMapForm.dehydrate();
        //window.scope_MasterPageForSlide.model = window.scope_TrackingMapForm.model;
        DSS5_SupplyChainFinancialsOptimisation.ViewModels.TrackingMapForm.TrackingMapFormViewModel._lightCast(window.scope_TrackingMapForm.model);
        // Joove.Common.applyScope(window.scope_TrackingMapForm);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: ITrackingMapFormScope, $timeout: ng.ITimeoutService) {
        window.scope_TrackingMapForm = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DSS5_SupplyChainFinancialsOptimisation.ViewModels.TrackingMapForm.TrackingMapFormViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_MasterPageForSlide._validations;
        $scope._validations = { summary:
                                [],
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: ITrackingMapFormScope, $timeout: ng.ITimeoutService) {


        $scope.model = DSS5_SupplyChainFinancialsOptimisation.ViewModels.TrackingMapForm.TrackingMapFormViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_MasterPageForSlide.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);


        //}

        $.connection['eventsHub'].on('__connectedEvent');

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {

MapPointDataset:
            (partialViewControlName: string = null) => {
                return new DSS5_SupplyChainFinancialsOptimisation.Controllers.TrackingMapFormMapPointDatasetViewModelDataset($scope.model, partialViewControlName);
            },
        };
// Controller actions
        $scope.actions = {
Render:
            async (orderId, _cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("TrackingMapForm", "Render", "GET", [orderId], {}, _modalOptions);
            },
DisplayDirections:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();

                const implementation = async (): Promise<any> => {

                    // Joove.Common.applyScope(window.scope_TrackingMapForm);
                }

                await window._ruleEngine.update(Joove.EvaluationTimes.OnSubmit);

                let spamHelper = new Joove.SpamControlHelper(_e);
                spamHelper.Disable();
                await implementation();
                spamHelper.Enable();
                this.controllerActionFinished(true);
            },
ShowRoute:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();

                const implementation = async (): Promise<any> => {
                    Joove.Common.nullSafe<any>(() => window._commander.mapDirections(["NewMap", Joove.Common.nullSafe<any>(() => window["scope_TrackingMapForm"].model.MapPoints[0].Address, ""), Joove.Common.nullSafe<any>(() => window["scope_TrackingMapForm"].model.MapPoints[Joove.Common.safeNumber(Joove.Common.nullSafe<any>(() => window["scope_TrackingMapForm"].model.MapPoints.length, 0)) - Joove.Common.safeNumber(1)].Address, "")]), null);


                    // Joove.Common.applyScope(window.scope_TrackingMapForm);
                }

                await window._ruleEngine.update(Joove.EvaluationTimes.OnSubmit);

                let spamHelper = new Joove.SpamControlHelper(_e);
                spamHelper.Disable();
                await implementation();
                spamHelper.Enable();
                this.controllerActionFinished(true);
            },
GetOriginDestination:
            async (or, dest, _cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("TrackingMapForm", "GetOriginDestination", "GET", [or, dest], {}, _modalOptions);
            },
        };
// Events
        $scope.eventCallbacks = {
ButtonClicked:
            (e, DataItem, _parents) => {
                window._recorder.addState(e);

                if (e != null) {
                    e.stopPropagation();
                    e.preventDefault();
                }

                if (this.timeoutDelayLockButtonClicked != null) {
                    $timeout.cancel(this.timeoutDelayLockButtonClicked);
                }

                this.timeoutDelayLockButtonClicked = $timeout(() => {
                    Joove.Common.setControlKeyPressed(e, 0); Joove.Common.setLastClickedElement(e);

                    Joove.Validation.Manager.validateFormAndExecute(() => {
                        $scope.actions.DisplayDirections(null, null, e);
}, { groups: [Joove.Validation.Constants.ALL_GROUPS], withDataValidationsCheck: true, withRequiredFieldsCheck: true });
                }, 0);
            },

        };

// Rules
        window._ruleEngine.addDataValidations(TrackingMapFormDataValidations);
        window._ruleEngine.addConditionalFormattings(TrackingMapFormConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(TrackingMapFormCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_MasterPageForSlide as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("TrackingMapFormController", ["$scope", "$timeout", TrackingMapFormController] as Array<string>);
}
