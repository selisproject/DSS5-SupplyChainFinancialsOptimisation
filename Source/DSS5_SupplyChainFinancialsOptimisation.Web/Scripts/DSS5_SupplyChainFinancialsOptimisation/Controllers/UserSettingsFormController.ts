// Copyright (c) CLMS. All rights reserved.
// Licensed under the AGPL-3.0 license. See LICENSE file in the project root for full license information.
// This source file was autogenerated by zAppDev(r).
var directiveScopeReadyLimit = 10;
var directiveScopeReadyTimeout = 200;
window.onbeforeunload = (e) => {
    if (window._context.isDirty) {
        var msg = window._resourcesManager.getGlobalResource("RES_WEBFORM_DirtyMessage");
        e.returnValue = msg;     // Gecko, Trident, Chrome 34+
        return msg;
    }
};
interface Window { scope_UserSettingsForm: DSS5_SupplyChainFinancialsOptimisation.Controllers.IUserSettingsFormScope; }
namespace DSS5_SupplyChainFinancialsOptimisation.Controllers {
const UserSettingsFormConditionalFormattings = [
        ];
const UserSettingsFormDataValidations = [
                                        ];
const UserSettingsFormCalculatedExpressions = [
        ];
// Manual Data for DropdownBox
window["dataForDropdownBox"] = [ {
_key: "Euro",
_text: window._resourcesManager.getComboBoxManualValueDisplay('CurrencyDataSet', '0', false , null),
_default: false
}, {
_key: "Dollar",
_text: window._resourcesManager.getComboBoxManualValueDisplay('CurrencyDataSet', '1', false , null),
_default: false
} ]
// Manual Data for DropdownBox1
window["dataForDropdownBox1"] = [ {
    _key: 1,
_text: window._resourcesManager.getComboBoxManualValueDisplay('DateFormat', '0', false , null),
_default: false
}, {
    _key: 2,
_text: window._resourcesManager.getComboBoxManualValueDisplay('DateFormat', '1', false , null),
_default: false
}, {
    _key: 3,
_text: window._resourcesManager.getComboBoxManualValueDisplay('DateFormat', '2', false , null),
_default: false
} ]

// Datasets Column Info
export var CurrencyDataSet_ColumnInfo = [new Joove.ColumnInfo("Value", "string", null,  true)];
window["CurrencyDataSet_ColumnInfo"] = CurrencyDataSet_ColumnInfo;
export var DateFormat_ColumnInfo = [new Joove.ColumnInfo("Value", "string", null,  true)];
window["DateFormat_ColumnInfo"] = DateFormat_ColumnInfo;

// Datasets Projection Shemas




export interface IUserSettingsFormScope extends Joove.IWebPageScope {
model:
    DSS5_SupplyChainFinancialsOptimisation.ViewModels.UserSettingsForm.UserSettingsFormViewModel;
_partialModelStructure:
    any;
    _validations?: {
summary: Array<Joove.Validation.BindingEntry>,

    },
    _masterValidations?: any;
}

class UserSettingsFormController {


    private controllerActionFinished = (resetDirty: boolean) => {
        //window.scope_UserSettingsForm.model = window.scope_UserSettingsForm.dehydrate();
        //window.scope_MasterPage.model = window.scope_UserSettingsForm.model;
        DSS5_SupplyChainFinancialsOptimisation.ViewModels.UserSettingsForm.UserSettingsFormViewModel._lightCast(window.scope_UserSettingsForm.model);
        // Joove.Common.applyScope(window.scope_UserSettingsForm);
        if (resetDirty) {
            window._context.isDirty = false;
        }
        window._ruleEngine.update(Joove.EvaluationTimes.OnChange);
    }

    constructor(private $scope: IUserSettingsFormScope, $timeout: ng.ITimeoutService) {
        window.scope_UserSettingsForm = $scope;
        $scope.$onControlChanged = function (event, newValue, dontMakeDirty) {
            Joove.Core.onChange(event.target, newValue, dontMakeDirty)
        }

        $scope.dehydrate = (context = null) => DSS5_SupplyChainFinancialsOptimisation.ViewModels.UserSettingsForm.UserSettingsFormViewModel._initializeFrom($scope.model, context);
        $scope._validationsMaster =  window.scope_MasterPage._validations;
        $scope._validations = { summary:
                                [],
                              };

        this.init($scope, $timeout);
        $timeout(() => {

            window._ruleEngine.update(Joove.EvaluationTimes.OnLoad, null, () => {

                $("body").fadeIn(200);

            });
        });

    }

    init($scope: IUserSettingsFormScope, $timeout: ng.ITimeoutService) {


        $scope.model = DSS5_SupplyChainFinancialsOptimisation.ViewModels.UserSettingsForm.UserSettingsFormViewModel._initializeFrom(window.viewDTO.Model);

        window.scope_MasterPage.model = $scope.model;

        new Joove.ReferencesReconstructor().reconstructReferences($scope.model);
        $scope.trackObject = obj => Joove.Common.trackObject(obj);


        //}

        $.connection['eventsHub'].on('__connectedEvent');

        $.connection['eventsHub'].on('forcePageReload', () => {
            window.onbeforeunload = null;
            setTimeout(() => {
                window.location.reload();
            }, 3000);
        });
        $.connection.hub.start(() => {
            Joove.Common.getScope().connectedToSignals();
            Joove.Common.getMasterScope().connectedToSignals();

        });
        $scope.connectedToSignals = () => {
        }
// Event Listeners
        $scope.events = {
        };
        $scope.expressions = {
        }
// Dataset Handler
        $scope.datasets = {
        };
// Controller actions
        $scope.actions = {
Render:
            async (_cb, _modalOptions, _e) => {
                Joove.Common.autocompleteFix();


                Joove.Core.executeRedirectControllerAction("UserSettingsForm", "Render", "GET", [], {}, _modalOptions);
            },
        };
// Events
        $scope.eventCallbacks = {
        };

// Rules
        window._ruleEngine.addDataValidations(UserSettingsFormDataValidations);
        window._ruleEngine.addConditionalFormattings(UserSettingsFormConditionalFormattings);
        window._ruleEngine.addCalculatedExpressions(UserSettingsFormCalculatedExpressions);

        window._commander.executeCommands(window.viewDTO.ClientCommands);
        window.viewDTO.ClientCommands = [];

        Joove.Common.setNumberLocalizationSettings();
        Joove.DeveloperApi.init($scope as any, window.scope_MasterPage as any);
        window.$formExtend && window.$formExtend();
        window.$onFormLoaded && window.$onFormLoaded();
    }
}
angular.module("Application").controller("UserSettingsFormController", ["$scope", "$timeout", UserSettingsFormController] as Array<string>);
}
